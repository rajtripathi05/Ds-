# ASSUMPTIONS — ds-doc-to-decision build
| # | Ambiguity | Decision | Why defensible |
|---|-----------|----------|----------------|
| D1 | pdfplumber uninstallable (pip 403) | Hand-rolled PDF text extractor: ASCII85+Flate content-stream decode (stdlib base64/zlib) + text-operator parse with y/x positioning — sufficient for these machine-generated PDFs; production uses pdfplumber/OCR per kickoff | Override 5 names pdfplumber; nearest offline equivalent, logged |
| D2 | Mount rejects .git (proven in both prior builds) | separate-git-dir + ds-doc-to-decision.gitbundle at every milestone | Same durable-history pattern |
| D3 | M6 degradation via pypdfium2/PIL rasterization — no PDF rasterizer offline (PIL present, but nothing reads text back from images without OCR) | Degradation applied in the TEXT domain at increasing intensity (char drops/substitutions o↔0 l↔1, whitespace corruption, line truncation) → accuracy-vs-noise measured through the SAME extractor+pipeline → robustness.png | Measures the pipeline's real tolerance to input corruption; honestly labelled |
| D4 | M4 calibration needs labels but GT files are scoring-only | Calibration target is GT-FREE: P(PDF-path decision == structured-JSON-path decision), fitted on a holdout split of the 40 sets (isotonic, hand-rolled); GT enters only in tests/ scoring scripts | Stricter than required; decision path stays label-blind, enforced by grep gate |
| D5 | Rule 8 "extraction confidence >= threshold" initial value | Start conservative at 0.90 (min field confidence per doc); the M5 learning loop then re-tunes it from override outcomes — which is exactly the demonstration the kickoff wants | Conservative start + measured adaptation |
| D6 | "GST arithmetic within ₹1" — GST rate source | Recompute from invoice's own subtotal & gst fields: grand_total ?= subtotal + gst (±₹1), and gst ?= subtotal × rate where rate parsed from the document (e.g. "IGST 18%"); both checks named separately | Uses only in-document facts; matches rule 7 text |
