# CLAUDE.md — ds-doc-to-decision (rules for any agent here)
LAWS: (1) RULES ARE TRUTH — src/match.py implements data/matching_rules.md verbatim; extraction NEVER decides; every decision carries the named failing check. (2) CONFIDENCE GATE — calibrated per-document confidence; below threshold → human queue with the exact failing/uncertain field; never a silent post. (3) LEARNING LOOP — every override logged (immutable audit: inputs, checks fired, confidence, actor) and re-tunes the gate; STP must be shown rising with measured numbers. (4) NEVER STOPS — unreadable/missing → flag + route with reason.
ANTI-FABRICATION (tonight's override 4): ground_truth_labels.csv and planted_discrepancies.csv are SCORING-ONLY — no decision-path module may reference them; tests/gate_no_peek.py greps src/ for those names and fails the build if found. Commit messages may claim GREEN only from a captured gate exit code (lesson from ds-copilot).
DISCIPLINE: data/ read-only; measured numbers only; PROGRESS heartbeats; honest reds in BLOCKERS.md.
