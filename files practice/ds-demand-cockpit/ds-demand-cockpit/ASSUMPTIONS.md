# ASSUMPTIONS — ds-demand-cockpit build (every judgment call)
| # | Ambiguity | Decision | Why defensible |
|---|-----------|----------|----------------|
| B1 | pip blocked (403): statsforecast/lightgbm/scipy/sklearn/flask/pytest/playwright unavailable | Hand-rolled numpy equivalents: seasonal-index ETS-lite (EWMA level+trend on deseasonalised series) for AutoETS; closed-form global ridge regression for LightGBM; Croston + TSB implemented directly; stdlib http.server for Flask; plain-python test runner for pytest; MinT(OLS) via numpy projection | Kickoff override #5 permits nearest-equivalent substitutions, logged |
| B2 | 9,060 SKU×distributor series too slow for per-series offline fitting | Model at SKU×zone grain (≤906 series); distributor views via trailing 8-wk share allocation. Gates still evaluated at SKU (nation) and category level per kickoff | Forecast where signal lives, allocate down; gates unchanged |
| B3 | No browser (Playwright uninstallable) | M6 UI verification = node DOM-shim run of cockpit JS + live HTTP smoke of every API incl. role gate + matplotlib PNGs of cockpit views into ./screenshots/ | Honest offline maximum; recorded in BLOCKERS.md |
| B4 | "xlsx + PDF" export, no PDF lib | xlsx (openpyxl) + print-styled HTML pack | Nearest equivalent, logged |
| B5 | No scipy.linprog | Separable linear allocation solved exactly by marginal-value greedy under budget/capacity/service caps; binding constraint = exhausted cap; shadow price via +1% budget re-solve | Provably optimal for this separable structure; deterministic; testable |
| B6 | No production-capacity field in dataset | Category weekly capacity = 1.15 × trailing-8wk mean primary (stated in UI as assumption) | Real constraint without inventing data fields |
| B7 | Building in the original repo folder | data/ read-only; only code/docs added | Kickoff names this folder as repo root |
| B8 | Mount rejects .git (lockfile writes garbled; husk undeletable due to cloud-sync locks) | GIT_DIR in sandbox + GIT_WORK_TREE on mount; `ds-cockpit.gitbundle` refreshed at every milestone commit in repo root — single-file, clone-able full history on the user's machine | Durable, runnable-at-every-commit history despite mount limitation |
| B9 | Promo ground truth needed for M3 gate | From generate_ds.py (read-only): lift = 1 + depth×|elasticity|×2.0×type_mult; type_mult consumer=1.0 / trade=0.7 / display=0.5; per-SKU elasticity truth = product_master.base_elasticity | The documented generative recipe IS the validation target |
| B10 | near_stockout is False on ALL 377,442 inventory rows (order-up-to policy keeps weeks_cover >= 1.0; min unflagged cover = 1.0) | Reconciliation gate interpreted as: risk detector must reproduce the flag set exactly on the data's own definition (cover < 1.0) -> both empty; forward-looking risk emerges only under constrained plans and is reported separately | Uses the dataset's definition rather than inventing stockouts; measured, not assumed |
| B11 | Sandbox executes each shell call in an isolated PID namespace with a hard 45 s cap — no background jobs, so the full sequential CI cannot run in one call here | tests/run_tests.py gained --only <gate>/assemble: each gate executed in its own call this session, measured evidence recorded to out/cache/ci_state.json, then assembled into CI_RESULTS.md. On a normal machine `python tests/run_tests.py` runs the identical suite in one go | Same tests, same assertions, same measured numbers; only the invocation is sliced. Nothing is skipped or mocked |
