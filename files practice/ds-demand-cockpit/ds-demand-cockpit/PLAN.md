# PLAN — ds-demand-cockpit (pre-approved per overnight instruction; recorded for the audit trail)
**Objective.** Hierarchical demand forecasting + causal trade-promo uplift + constrained S&OP optimizer + live what-if cockpit on the frozen 297,610-row synthetic spine — every milestone gated by measured tests (M0–M6 as in KICKOFF_PROMPT.md, gates restated in tonight's instruction).
**Approach.** Offline-only stack (pandas/numpy/matplotlib/openpyxl + stdlib; substitutions in ASSUMPTIONS B1). Pipeline writes one read-only plan snapshot under out/plan (ONE-WAY STATE); cockpit and analyses only read it. What-ifs are INTENTS: overlay factors → deterministic re-reconcile + re-optimize → only then displayed (RECONCILIATION IS TRUTH). Loaders never die: warn + fallback + completeness badge (NEVER STOPS), proven against the messy files.
**Affected files.** src/{loaders,features,models,backtest,hierarchy,fva,causal,optimizer,pipeline,server}.py · cockpit/index.html · tests/run_tests.py · out/plan/* · BACKTEST.md · README.md · MORNING_REPORT.md. data/ is read-only.
**Risks.** (1) Mount+git quirks → B8 bundle strategy. (2) Offline model quality vs gate: mitigate with feature-rich global ridge + per-series champion selection; if a gate stays red after 5 approaches → BLOCKERS.md, honestly. (3) What-if <2s: precompute base tensors, overlays are vectorised numpy. (4) OneDrive write latency on big artifacts → keep snapshots compact (parquet-not-available → npz/csv.gz).
