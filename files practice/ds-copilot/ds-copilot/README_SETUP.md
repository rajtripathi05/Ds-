# ds-copilot — setup (3 steps)
1. You unzipped this — done. Keep this folder as the repo root.
2. Put your IGL `CLAUDE.md` into `reference/` (operating-discipline example for Claude).
3. Open Claude Code in THIS folder and paste the contents of `KICKOFF_PROMPT.md`.
Claude will reply with a PLAN + a new CLAUDE.md → you approve → it builds.
