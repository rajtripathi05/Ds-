# Python
__pycache__/
*.py[cod]

# Local verification artifacts (generated by running the prototypes locally)
.playwright-mcp/
/cockpit.png
/cockpit-scenario-result.png
/copilot.png
/copilot-answer.png
/invoice-robot.png

# Never commit a real API key. openrouter_key.txt is where the live OpenRouter
# key gets pasted, so it is never tracked. Copy openrouter_key.example.txt to
# openrouter_key.txt and paste your key there; it stays local.
**/openrouter_key.txt
.env
